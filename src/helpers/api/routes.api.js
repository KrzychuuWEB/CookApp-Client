import { API_URL } from "../constants";

export const loginUserURL = `${API_URL}/login_check`;
export const RegisterUserURL = `${API_URL}/users`;
export const RefreshTokenUserURL = `${API_URL}/token/refresh`;